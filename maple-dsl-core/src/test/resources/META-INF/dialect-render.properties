# suppress inspection "UnusedProperty" for whole file
traversal= START \
  FROM <from:{it|"<it>"}; separator=","> \
  OVER <over; null="*", separator=","> \
  SELECT <[selection,shadow_selection]; separator=",">;